<image src="https://c.tenor.com/DJJogP4rg6wAAAAC/madagascar-private.gif" width="100%" />

```zsh
git clone git@github.com:grimmbraten/.grimmbraten.git ~/.grimmbraten && ln -s ~/.grimmbraten/.zshrc ~/.zshrc && ln -s ~/.grimmbraten/.gitconfig ~/.gitconfig
```
